{
  "name": "lynk-webhook-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Aurora backend (Lynk webhook + NocoDB + email)",
  "main": "webhook.js",
  "scripts": {
    "start": "node webhook.js",
    "dev": "node --watch webhook.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "nodemailer": "^6.9.16",
    "resend": "^4.0.0"
  }
}
